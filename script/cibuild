#!/usr/bin/env bash

# Halt script on error.
set -e;

# Build the Jekyll site and run the HTML proofer.
bundle exec jekyll build;
bundle exec htmlproof ./_site --disable-external;

# Run Intern.js tests.
../node_modules/.bin/intern-runner config=tests/intern/intern;

